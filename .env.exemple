# Variables d'environnement à ajouter sur Render (backend)

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:alerts@amaterasu.app

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL_FROM="Amaterasu Alertes <alertes@amaterasu.app>"

# Génération des clés VAPID (PowerShell, une commande à la fois) :
#   npx web-push generate-vapid-keys
